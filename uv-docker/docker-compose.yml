services:
  app:
    build:
      context: .
      args:
        PYTHON_VERSION: "3.12"
    image: uv-docker
    command: ["uv", "run", "--frozen", "python", "main.py"]
    volumes:
      - .:/app
      - /app/.venv
